```python
Q1. Explain the following with an example?
A)Artificial Intelligence
B)Machine Learning
C)Deep Learning

Ans-
  A) Artificial Intelligence (Ai) is a broad field of computer science that focues on developing intelligent machine
    
    Example:An AI-powered virtual assistant like siri or Alexa that can understand and respond to user voice commands
    
    B)Machine Learning model that predicts  whether a customer training machines to learn from data, recognize patterns
    
    Example: A machine learning model that predicts-whether a customer will buy a product based on their browsing history
    
    C)Deep learning is a subset of machine-learning that involves training neural networks with multiple layers to perform
    
    Example:A deep learning model that can accurately recognize different breeds of dogs from images by analyzing patterns
```


```python
Q2. What is supervised learning? List some example of supervised learning?
Ans-
   supervised learning is a type of machine learning where the algorithm learns from labeled data,meaning data
    
some example of supervised learning include:
    
1.Classification problems:where the goal is to predict a categorical output,such as whether an email s spam or not.

2.Regression problems:where the goal is to predict a continuous ouput,such as the price of a house based on its feature

3.Natural Language Processing(NLP) problems:where the goal is to classify text data into different categories

Examples of supervised learning algorithms include linear regression ,logistic regression,decision trees, support vector


```


```python
Q3.What is unsupervised learning? List some examples of unsupervised learning.
Ams-
    Unsupervised learning is a type of machine learning where the algorithm learns patterns and relationships in the data
    
Clustering:Algorithms like k-means clustering or hierarchial clustering can group similar data points together based 

Dimensionally reduction:Algorithms like principle component  analysis(PCA) or t-SNE can reduce the number of features

Anomly detection:Alogorithms like Isolation Forest or Local outlier Factor can identify data points that deviate significant

Association rule learning: Algorithm like Apripori or Fp-growth can identify rules and patterns in transactional data

Generative models: Algorithms like autoencoders or generative adversarial networks(GANs) can learn to generate new data
```


```python
Q4.What is the difference between AI,ML,DL and DS?
ANs-
  Al(Atificial In Intelligence)refers to the broader field of creating machines that can perform tasks that typically
    
ml (Machine Learning) is a subset of AI that involves training machines to learn from without being explicitly

DL (Deep Learning) is a subset of machine learning that uses neural networks with multple layers to learn hierarchial

DS (Data Science) is an interdisciplinary field that involves extracting insights and knowledge from data

In summary, AI is a broad field od study that includes ML , and DL as subfields , while Ds involving various methods
```


```python
Q5.What are the main difference between supervised, unsupervised and semi-supervised learning ?
Ans-
   The main differences between supervised, unsupervised and semi-supervised learnings are:
    
1.Supervised Learning: In supervised learning, the machine learning algorithm learns from labeled data

2.Unsupervised Learning: in unsupervised learning, the machine learning algorithm learns from a combination of labeled data

3.Semi-supervised learning: In semi-superviesed learning,the machine learning algorithm learns from a combination of labeled data

In summary, the mai difference between these type of learning is the type of data used to train the machine larning.
```


```python
Q6.What is train, test and validation test?  Explain the importance of each time
Ans-
  In machine leraning , the dataset is divided into three subsets: training set , validatation setset and test set
    
1.Training set:This set is the subset of the dataset used to train the model. The model is trained on this set of data

2.Validatation set: This is the subset of dataset used to tune the model's hyperparameters, 

3.Test set: This is the subset od dataset used to evaluate the final performaces of the trained model. 

The importance of each terms:

Training set: used to train the model by adjusting its parameters to minimize the error or loss function
Validaton set:nused to tune the model's hyperparameters to improve its performaces on new data and to prevent pverfitt

Test set: used to evaluate the final performaces of the trained model on new, unseen data.

Proper splitting f the dataset into three subset ensures that model is not overfitting and underfitting 
    
```


```python
Q7. How can unsupervisedlearning be used in anomly detection?
Ans-
    Unsupervised learning can be used in anomly detection by traning a model on a dataset without labels and identifying
    
one common unsupervisedtechnique for anomly clustering where the data is divided into group based on

Another technique is density- based anomly detection here the model learns the density distribution of the data

Overall, unsupervised learning can be powerfull tool for anomly detection, especially in case where labeled data
```


```python
Q8.List down some commonly used supervised learning algorithm and unsupervised learning alorithms.
Ans-
   Some commonly used supervised learning agorithms:
        
1.Linear Regressions
2.Logistic Regression
3.Decision trees
4.Rndom Forest
5.Naive Bayes
6.K-Neraest Neibhors
7.Gradient boosting
8.supportive vector machines

some commonly used unsupervised learning algorithms are:
    
1.k-means clutering
2.Hierarchical Clutering
3.principle Component Analysis(PCA)
4.t-distributed stochastic Neighbor Embedding (t-SNE)
5.Autoencoders
6.Generative Adversarial Networks (GANs)
7.self-organizing Maps(SQMs)



```
